{
  "name": "t-dev-600-par-22",
  "private": true,
  "scripts": {
    "test:front": "npm --prefix frontend test -- --runInBand",
    "test:back": "cargo test --manifest-path backend/Cargo.toml",
    "test": "npm run test:front && npm run test:back",
    "coverage:front": "npm --prefix frontend test -- --runInBand --coverage",
    "coverage:back": "SQLX_OFFLINE=true cargo tarpaulin --manifest-path backend/Cargo.toml",
    "coverage": "npm run coverage:front && npm run coverage:back"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2"
  }
}
